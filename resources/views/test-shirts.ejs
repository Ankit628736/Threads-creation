<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Test Shirts Page</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
    <div class="container mx-auto py-8">
        <h1 class="text-4xl font-bold text-center mb-8">Shirts Test Page</h1>
        
        <% if(pizzas && pizzas.length > 0) { %>
            <p class="text-center text-green-600 mb-6">✅ Found <%= pizzas.length %> shirts in database</p>
            
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
                <% pizzas.forEach(function(shirt, index) { %>
                    <div class="bg-white rounded-lg shadow-md p-4">
                        <img src="/img/<%= shirt.image %>" alt="<%= shirt.name %>" class="w-full h-48 object-cover rounded mb-4">
                        <h3 class="text-lg font-semibold mb-2"><%= shirt.name %></h3>
                        <p class="text-gray-600 mb-2">Size: <%= shirt.size %></p>
                        <p class="text-green-600 font-bold text-xl">₹<%= shirt.price %></p>
                        <% if(shirt.category) { %>
                            <span class="inline-block bg-blue-100 text-blue-800 text-xs px-2 py-1 rounded mt-2">
                                <%= shirt.category %>
                            </span>
                        <% } %>
                        <% if(shirt.description) { %>
                            <p class="text-gray-500 text-sm mt-2"><%= shirt.description %></p>
                        <% } %>
                    </div>
                <% }) %>
            </div>
        <% } else { %>
            <div class="text-center">
                <p class="text-red-600 text-xl">❌ No shirts found!</p>
                <p class="text-gray-600">Database might be empty or there's a connection issue.</p>
            </div>
        <% } %>
    </div>
</body>
</html>